Message repair: the word "measured" was lost from the final paragraph by shell
command substitution, because the message was passed inline and the term was
wrapped in backticks. The sentence should read:

    ... and the reading stays `measured` because the lane was read
    successfully -- what is withheld is advice, not data.

That is the `state` field's value, so the sentence as committed drops the name
of the thing it is about. Nothing in the code or the tests is affected.
