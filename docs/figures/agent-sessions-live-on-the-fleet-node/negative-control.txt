before the edit, grep -c 'waits for a 500' on the note returns 1
before (git show HEAD:docs/research/fleet-view-rendering.html): 1
after  (working tree docs/research/fleet-view-rendering.html): 0
the declared mutation is a text substitution in a research note, not a test path;
the counts above are the provenance for the EVIDENCE fence's `grep -c 'waits for a 500' returns 0` clause